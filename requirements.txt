# ---- Core API ----
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2

# ---- Database (local, file-based) ----
sqlalchemy==2.0.35
# Using SQLite by default (100% local, zero-config).
# Swap to psycopg2-binary + a local Postgres container if you prefer Postgres.

# ---- OCR / Document Parsing (all local) ----
pytesseract==0.3.13          # wraps local Tesseract OCR binary
pdf2image==1.17.0            # needs local poppler-utils
Pillow==10.4.0
python-docx==1.1.2
openpyxl==3.1.5
pandas==2.2.3

# ---- Apache Tika (runs as a local JAR, no cloud calls) ----
tika==2.6.0

# ---- Embeddings + Vector DB (fully local) ----
sentence-transformers==3.1.1
chromadb==0.5.15

# ---- Local LLM runtime ----
# Ollama runs models (llama3, mistral, phi3, etc.) 100% on your machine.
# We talk to it over localhost HTTP - no external API keys, no internet needed.
requests==2.32.3

# ---- Utilities ----
python-dotenv==1.0.1
numpy==1.26.4
